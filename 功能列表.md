## 目前项目的功能

### 1) 输入（两种形态）

- requirements：粘贴自然语言需求/业务规则
- codebase：粘贴接口文档/校验规则/代码片段（当作“可观察规则来源”）

### 2) 选择测试技术（四种黑盒方法）

- `ep_bva`：等价类划分 + 边界值分析（工业化结构：含代表值、BVA 表行、覆盖率总结）
- `decision_table`：判定表测试（结构化输出 conditions/actions/rules + 用例 covers_rule）
- `state_transition`：状态迁移测试（结构化状态机 states/events/transitions + 路径 steps）
- `combinatorial`：组合/Pairwise（结构化 factors/constraints/coverage_strategy/coverage_summary + combination/covers_pairs）

### 3) 输出

- 网页右侧展示最终 JSON
- 后端会补充 `meta`（记录 technique/model/temperature，且可选 input_type/prompt_version，便于做实验对比）
- 后端会做归一化和兜底去冲突（例如 combinatorial 出现“同组合不同预期”会被剔除并写 notes）

### 4) 运行方式

- `uvicorn app.main:app --reload`
- 浏览器打开 `http://127.0.0.1:8000`
- 选择 Input Type / Technique / Model / Temperature / Prompt Version（默认 v4）→ Generate

------

## 提示词调优

- - ## 1) 各技术策略的提示词设计

    ###  EP/BVA（Equivalence Partitioning + Boundary Value Analysis）

    目标：生成可执行、可复核的 EP/BVA 测试设计，不是泛泛列点。

    - 输入变量识别策略
      prompt 要求列出变量，并在 `notes` 里写清：
      - 类型（enum/boolean/number/string）
      - 合法范围/枚举值
      - 依赖关系（跨字段约束）
    - EP 生成策略（工业化）
      - 每个变量必须有 ≥1 valid EP + ≥1 invalid EP
      - 每个 EP 必须包含 `representative_values`（至少 1 个具体值）
      - 跨字段规则用“组合等价类”表示（valid=false，代表值用完整输入对象）
    - BVA 生成策略
      - 有序类型必须出：min−1/min/min+1/max−1/max/max+1，并标注期望有效性
      - 枚举类型必须明确声明 “无有序边界”（避免硬编边界）
    - 测试用例覆盖逻辑
      - 每个 valid EP 至少一个成功用例
      - 每个 invalid EP（含组合 EP）至少一个失败用例
      - 每条边界行必须至少一个专用用例
      - scenario 显式标注覆盖的 EP ID / 边界行信息
    - 覆盖率总结
      - 输出 `coverage_summary`（总 EP/覆盖 EP、总边界/覆盖边界）

    ------

    ###  Decision Table

    目标：从“规则样例集”升级为“规则矩阵驱动”的测试设计。

    - 建模策略：条件桩/动作桩/规则列
      - `conditions[]`：每个条件带 `values`（true/false 或有限枚举）
      - `actions[]`：动作不止 success/reject，而是“具体输出/错误码”
      - `rules[]`：用 `condition_entries`（按 conditions 顺序）和 `action_entries`（按 actions 顺序）
      - 支持 `-`（don’t care）用于化简/合并规则列
    - 用例派生策略
      - 每条 rule 至少一个 test case
      - `covers_rule` 明确映射
      - expected 必须可执行（HTTP code + field + error_code）
    - 禁止串味
      - decision_table 输出不允许出现 EP/BVA/组合/状态字段（由后端强校验保证）

    ------

    ###  Combinatorial / Pairwise

    目标：真正的 pairwise 覆盖，不是“每次只改一个字段”。

    - 参数建模策略
      - `parameters[]`：列出所有输入参数的有效取值，并标注 type（enum/boolean/numeric_range）
      - 无效值（非法枚举/格式）不纳入 pairwise 参数集合，作为单独负测处理
    - 约束建模策略
      - `constraints[]`：显式列出业务约束导致的无效组合，并给出 `invalid_combinations` 示例
      - 生成用例时必须跳过无效组合
    - pairwise 覆盖策略
      - 最小化用例集，目标覆盖任意两参数取值组合
      - 输出 `coverage`：total_pairs/covered_pairs/coverage_percent，并说明因约束无法覆盖的 pair
    - 冲突修复（工具兜底）
      - 如果模型生成“相同 inputs 不同 expected”，后端会自动剔除冲突用例，并把说明写进 `coverage.notes`（保证交付数据一致）

    ------

    ###  State Transition

    目标：输出真正状态机，而不是“输入校验穿个 from/to 外衣”。

    - 适用性判断策略
      - prompt 强制先判断是否有状态性（生命周期、阶段、同事件不同状态不同结果、超时/重试/回退等）
      - 若无状态性：输出标准 error JSON（不硬做）
    - 状态机建模策略
      - `states/events/transitions` 全部结构化输出
      - transitions 体现 guard、输出、副作用，并覆盖正/负向转移（非法事件/错误状态）
    - 测试用例策略
      - test case 明确 initial_state + event + context → expected_state/output
      - coverage 给出总转移数、覆盖数、未覆盖列表
    - 禁止串味
      - state_transition 不允许出现 EP/BVA/decision table/组合字段（后端强校验）

    ------

    ## 2) “不串味”的关键工程措施

    - Technique 专用输出 schema：后端按 technique 选择不同响应模型校验；多余字段直接拒绝。
    - Prompt 明确“只输出该技术字段”：每个 technique 段落都有禁止事项。
    - 归一化与去冲突：对组合测试做冲突清洗，避免同输入不同预期污染数据。





## 人工检查流程参考

- 结构合法性
  - 只有这些 key：`input_variables`, `equivalence_partitions`, `boundary_values`, `test_cases`, `meta`, `notes`
  - `test_cases` 每条都有：`id`,`scenario`,`inputs`,`expected`
- 技术一致性
  - `ep_bva`：必须同时有 valid/invalid 等价类 + 真实边界上下值（below/at/above）
  - `decision_table`：`notes` 里有条件/动作摘要；用例覆盖多规则组合
  - `state_transition`：`notes` 里有状态/事件/迁移摘要；包含无效迁移用例
  - `combinatorial`：`notes` 里列出选择的变量与值集；用例数量合理且覆盖 pairwise 思路
- 覆盖性
  - 主要输入变量是否都至少出现在某条用例 `inputs` 里
  - 每条关键约束是否都有至少 1 个“违反它的无效用例”
- 真实性
  - `expected` 不应凭空出现系统功能；不确定的要在 `notes` 写假设